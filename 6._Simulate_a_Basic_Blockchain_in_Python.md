# <a name="_yq4vn21sgxtq"></a>**Simulate a Basic Blockchain in Python**
To simulate a basic blockchain in Python by implementing:Blocks, Hash chaining, Proof-of-Work mechanism

-----
# <a name="_4vhgkewvs2bn"></a>**🔹 PART A: Install Python**
## <a name="_prl4r5p5arp"></a>**Step 1: Install Python**
- Download from: <https://python.org>
- During installation: ✔ Enable:
- Add Python to PATH
-----
# <a name="_593e9w192n7x"></a>**🔹 PART B: Create Python Program**
## <a name="_bxg0qbucjiia"></a>**Step 2: Open VS Code**
Open Visual Studio Code.-----
## <a name="_24e1s69dvbq1"></a>**Step 3: Create Folder**
Create folder:

blockchain-project

-----
## <a name="_52hogcmv1jh2"></a>**Step 4: Create Python File**
Create file: blockchain.py

-----
# <a name="_qtv8z1u03kp0"></a>**🔹 PART C: Final Complete Program**
## <a name="_if9c2wpobn5e"></a>**Step 5: Complete Code**
import hashlib

import datetime


class Block:

`    `def \_\_init\_\_(self, index, data, previous\_hash):

`        `self.index = index

`        `self.timestamp = str(datetime.datetime.now())

`        `self.data = data

`        `self.previous\_hash = previous\_hash

`        `self.nonce = 0

`        `self.hash = self.calculate\_hash()

`    `def calculate\_hash(self):

`        `value = (

`            `str(self.index)

`            `+ self.timestamp

`            `+ self.data

`            `+ self.previous\_hash

`            `+ str(self.nonce)

`        `)

`        `return hashlib.sha256(value.encode()).hexdigest()

`    `def mine\_block(self, difficulty):

`        `print("Mining block...")

`        `target = "0" \* difficulty

`        `while self.hash[:difficulty] != target:

`            `self.nonce += 1

`            `self.hash = self.calculate\_hash()

`        `print("Block mined:", self.hash)


class Blockchain:

`    `def \_\_init\_\_(self):

`        `self.chain = [self.create\_genesis\_block()]

`        `self.difficulty = 4

`    `def create\_genesis\_block(self):

`        `return Block(0, "Genesis Block", "0")

`    `def get\_latest\_block(self):

`        `return self.chain[-1]

`    `def add\_block(self, new\_block):

`        `new\_block.previous\_hash = self.get\_latest\_block().hash

`        `new\_block.mine\_block(self.difficulty)

`        `self.chain.append(new\_block)


my\_blockchain = Blockchain()

my\_blockchain.add\_block(

`    `Block(1, "Purva sends 10 coins to A", "")

)

my\_blockchain.add\_block(

`    `Block(2, "A sends 5 coins to B", "")

)

print("\nBlockchain:\n")

for block in my\_blockchain.chain:

`    `print("Block Index:", block.index)

`    `print("Timestamp:", block.timestamp)

`    `print("Data:", block.data)

`    `print("Previous Hash:", block.previous\_hash)

`    `print("Hash:", block.hash)

`    `print("Nonce:", block.nonce)

`    `print("-" \* 50)

-----
# <a name="_8611j35w0qmi"></a>**🔹 PART D: Execute Program**
## <a name="_kszlara14vul"></a>**Step 6: Open Terminal**
cd blockchain-project

-----
## <a name="_cc0yqofs34ye"></a>**Step 7: Run Program**
python blockchain.py

-----
Example Output:

- Mining block...

  Block mined: 0000ab34cd...

- Mining block...

  Block mined: 0000xy78ef...

- Blockchain:

  Block Index: 0

- Data: Genesis Block

  Block Index: 1

  Data: Purva sends 10 coins to A

  Block Index: 2

  Data: A sends 5 coins to B

-----

