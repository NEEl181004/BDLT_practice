
# <a name="_plw3nfugjql3"></a>**Crowdfunding Platform** 
Create a smart contract that allows users to create fundraising campaigns and accept donations. If the funding goal is not met by the deadline, donors should be able to withdraw their funds. 

-----** 

**STEP 1: CREATE FILE**

Open: Remix IDE

Create new file: Crowdfunding.sol-----

**STEP 2: PASTE FINAL CORRECT CODE**

// SPDX-License-Identifier: MIT

pragma solidity ^0.8.20;

contract Crowdfunding {

`    `struct Campaign {

`        `address payable creator;

`        `string title;

`        `uint256 goal;

`        `uint256 deadline;

`        `uint256 amountRaised;

`        `bool fundsClaimed;

`    `}

`    `uint256 public campaignCount;

`    `mapping(uint256 => Campaign) public campaigns;

`    `mapping(uint256 => mapping(address => uint256)) public donations;

`    `// 🚀 Create Campaign

`    `function createCampaign(

`        `string memory \_title,

`        `uint256 \_goal,

`        `uint256 \_durationInMinutes

`    `) public {

`        `campaignCount++;

`        `campaigns[campaignCount] = Campaign(

`            `payable(msg.sender),

`            `\_title,

`            `\_goal,

`            `block.timestamp + (\_durationInMinutes \* 1 minutes),

`            `0,

`            `false

`        `);

`    `}

`    `// 💰 Donate ETH

`    `function donate(uint256 \_campaignId) public payable {

`        `Campaign storage campaign = campaigns[\_campaignId];

`        `require(block.timestamp < campaign.deadline, "Campaign ended");

`        `require(msg.value > 0, "Send ETH");

`        `campaign.amountRaised += msg.value;

`        `donations[\_campaignId][msg.sender] += msg.value;

`    `}

`    `// 💸 Claim Funds

`    `function claimFunds(uint256 \_campaignId) public {

`        `Campaign storage campaign = campaigns[\_campaignId];

`        `require(msg.sender == campaign.creator, "Not creator");

`        `require(block.timestamp >= campaign.deadline, "Campaign still active");

`        `require(campaign.amountRaised >= campaign.goal, "Goal not reached");

`        `require(!campaign.fundsClaimed, "Funds already claimed");

`        `campaign.fundsClaimed = true;

`        `(bool sent, ) = campaign.creator.call{value: campaign.amountRaised}("");

`        `require(sent, "Failed to send Ether");

`    `}

`    `// 🔄 Refund

`    `function refund(uint256 \_campaignId) public {

`        `Campaign storage campaign = campaigns[\_campaignId];

`        `require(block.timestamp >= campaign.deadline, "Campaign still active");

`        `require(campaign.amountRaised < campaign.goal, "Goal was reached");

`        `uint256 donatedAmount = donations[\_campaignId][msg.sender];

`        `require(donatedAmount > 0, "No donation found");

`        `donations[\_campaignId][msg.sender] = 0;

`        `(bool sent, ) = payable(msg.sender).call{value: donatedAmount}("");

`        `require(sent, "Refund failed");

`    `}

`    `// 📊 Get Campaign Details

`    `function getCampaign(uint256 \_campaignId)

`        `public

`        `view

`        `returns (

`            `string memory title,

`            `uint256 goal,

`            `uint256 deadline,

`            `uint256 amountRaised

`        `)

`    `{

`        `Campaign memory c = campaigns[\_campaignId];

`        `return (

`            `c.title,

`            `c.goal,

`            `c.deadline,

`            `c.amountRaised

`        `);

`    `}

}

-----** 

**STEP 3: COMPILE**

Go to: Solidity Compiler

Select compiler version: 0.8.20

Click: Compile Crowdfunding.sol

✔ No warnings\
✔ No errors-----

**STEP 4: DEPLOY**

Go to:Deploy & Run Transactions

Environment: Remix VM (Cancun)

Click: Deploy

✔ Contract deployed successfully-----
# <a name="_ve1zkaue4jr1"></a> **STEP 5: CREATE CAMPAIGN**
Use function: createCampaign()

` `**EXACT INPUTS**

|**Parameter**|**Value**|
| :-: | :-: |
|\_title|"Medical Help"|
|\_goal|1000000000000000000|
|\_durationInMinutes|1|

-----
# <a name="_vqt13tif2zhz"></a> **IMPORTANT ETH → WEI**

|**ETH**|**WEI**|
| :-: | :-: |
|1 ETH|1000000000000000000|
|0\.5 ETH|500000000000000000|

So: 1000000000000000000

Means: ✔ 1 ETH goal
#
# <a name="_kzi1tt98okg0"></a><a name="_7sjhcm72j43i"></a>**✅ Click**
👉 transact

✔ Campaign created

-----
# <a name="_gqopzhjypxll"></a>**STEP 6: CHECK CAMPAIGN**
Use: getCampaign()

Input:1

Click: call

✔ Campaign details displayed-----
# <a name="_dgfmbdxkqi3k"></a>**STEP 7: DONATE ETH**
# <a name="_hlp0jhlu7uvf"></a>**🔥 VERY IMPORTANT PART**
Above contract functions in Remix you will see: VALUE

` `and beside it: wei ▼
# <a name="_yn2i293mfm1o"></a>**✅ CHANGE THESE**
## <a name="_5pz36gzdm2mk"></a>**VALUE:** Enter: 1
## <a name="_lm8tvun30jri"></a>**UNIT:** Click dropdown: wei ▼
Change to: ether-----
#
#
# <a name="_n3837bkl3mgk"></a><a name="_a7lajhgfowkr"></a><a name="_xs380ncmwcuk"></a>**⚠️ THIS IS THE IMPORTANT CHANGE**

|**Wrong**|**Correct**|
| :-: | :-: |
|1 wei|❌|
|1 ether|✅|

-----
# <a name="_n716j3g8okk3"></a>**NOW USE FUNCTION**
donate()

Input: 1

Click:  transact

✔ 1 ETH donated successfully

-----

**STEP 8: CHECK UPDATED CAMPAIGN**

Use:getCampaign()

Input: 1

Click: call

Now: amountRaised

will increase to: 1000000000000000000

meaning:\
✔ 1 ETH received

-----
# <a name="_i1drgply09pq"></a>**STEP 9: WAIT 1–2 MINUTES**
Because campaign duration was:

1 minute

Wait until the deadline finishes.

-----
# <a name="_745fw7ggoat3"></a> **STEP 10: CLAIM FUNDS**
Since goal reached:

Use: claimFunds()

Input:1

Click: transact

✔ Creator receives ETH-----
# <a name="_4u721bvoxmpt"></a>**OPTIONAL REFUND TEST**
If goal NOT reached:

Use: refund()

Input:1

Click: transact

✔ Refund processed

-----

